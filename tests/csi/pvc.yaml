apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-test-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cloudru-ssd
  resources:
    requests:
      storage: 1Gi
